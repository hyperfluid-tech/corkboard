version: '3.8'

services:
  blog:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: carbon_blog
    ports:
      - "8080:8080"
    volumes:
      - ../articles:/app/articles
      - ../config.toml:/app/config.toml
    environment:
      - RUST_LOG=carbon=info,axum=info
      - CARBON_PORT=8080
      - CARBON_ARTICLES_DIR=/app/articles
    restart: unless-stopped
